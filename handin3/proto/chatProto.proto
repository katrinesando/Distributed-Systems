syntax = "proto3";

package proto;
option go_package = "./proto";


service getCurrentTime {
    rpc serverBroadcastMessage (stream Broadcast) returns (AcK);
}
message Ack {
    int64 val = 1;
}

message Broadcast {
    string message = 1;
    Vector clock = 2;
}

